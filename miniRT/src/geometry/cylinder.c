/*
 * cylinder.c
 * Implements cylinder intersection and normal calculations
 */
#include "../../include/miniRT.h"


/*
 * Intersects a ray with a cylinder
 * Returns the intersection point and the normal at that point
 * If no intersection is found, return NULL
 * Otherwise return the intersection point
 */


t_intersection *intersect_cylinder(t_ray *ray, t_sphere *cylinder) {
    // TODO: Implement cylinder intersection using the ray-cylinder intersection formula
    // Return the intersection point and the normal at that point if an intersection is found
    // Otherwise return NULL

    // Example implementation using the ray-cylinder intersection formula
    // ...

    return NULL;
}